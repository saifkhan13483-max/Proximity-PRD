Fully optimize and production-deploy this full-stack app — frontend to Vercel, backend to Railway:

Frontend (Vercel):

Create a vercel.json at the project root with: SPA fallback rewrites (all routes → index.html), API proxy rewrites (/api/* → the Railway backend URL), and optimal cache-control headers (immutable for hashed assets, no-cache for index.html)
Update vite.config.ts so the Vite dev-server proxy reads the backend URL from VITE_API_URL env var, falling back to localhost:3001
Replace every hardcoded localhost:3001 API reference in frontend/src/services/ with a VITE_API_URL env-var base URL so it works in both dev and production
Add a .env.example listing all required frontend env vars: VITE_API_URL, VITE_SITE_URL, VITE_ANALYTICS_ID
Ensure the build command in vercel.json is npm run build with output dir dist and root dir frontend
Backend (Railway):

Create a railway.toml (or Procfile) in the backend/ folder pointing to node server.js
Replace the hardcoded PORT = 3001 with process.env.PORT || 3001 (Railway dynamically assigns the port)
Fix the CORS config: replace origin: true with an explicit allowlist reading from a ALLOWED_ORIGINS env var (comma-separated), so only the Vercel domain and localhost are accepted
Add app.set('trust proxy', 1) for Railway's reverse-proxy headers
Add a GET /health endpoint returning { status: 'ok', uptime: process.uptime() } for Railway's health checks
Critical — fix ephemeral storage: Railway's filesystem resets on every deploy, so users.json and contacts.json will be wiped. Migrate data persistence to Railway's PostgreSQL plugin (or at minimum add a clear warning comment and a startup check that re-seeds the admin user if the file is missing)
Move JWT_SECRET out of the hardcoded fallback string — throw a startup error if process.env.JWT_SECRET is not set
Add a .env.example in backend/ listing: PORT, JWT_SECRET, ALLOWED_ORIGINS
Shared / final steps:

Update replit.md with the production architecture: Vercel frontend URL proxies /api/* to Railway backend URL
Confirm npm run build in frontend/ produces zero TypeScript errors and a clean dist/ folder